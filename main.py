# Use assert, different types of imports, if __name__ == "__main__" & docstrings
#import

def menu():
    """This function acts as the CLI """
    # there should be a menu with welcome message, help/menu, quit, option to start program -> then enter postcode.
    # After postcode is entered, message should output saying if it is correct or not. If correct do a sleep(2s) then output long, lat -> then prompt user to enter radius

    while True:
        #"Enter postcode
        postcode = input("Please enter a Postcode")
        # validate postcode.
        try:
            pass
            # read through csv to obtain code
        except:
            pass




